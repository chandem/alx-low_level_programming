#include"main.h"
/**
*print_buffer -The function prints buffer.
*@b: it is input parameter.
*size: it is input parameter.
*/ 
void print_buffer(char *b, int size)
{
int i;
for(i=0;i<size && b[i]!='\0';i++)
{
return b[i];
}
}
