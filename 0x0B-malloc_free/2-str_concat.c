#include"main.h"
/**
*str_concat -function that concatenate two strings.
*@s1: input parameter.
*@s2: input parameter.
*Return: char 
*/
char *str_concat(char *s1, char *s2)
{
char *f;
unsigned i;
unsigned j;
for(i=0;*s1!='\0';i++);
for(j=0;*s2!='\0';j++);
f=malloc(i+j);
}
